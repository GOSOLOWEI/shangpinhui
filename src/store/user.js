//用于响应组件中的“动作”  —— 服务员
const actions = {}

//用于真正操作数据  —— 厨师
const mutations = {}

//初始化的数据 —— 原材料
const state = {
	name:'张三',
	age:18
}

//vuex层面的计算属性
const getters = {}

export default {
	actions,
	mutations,
	state,
	getters
}